package route

import "github.com/labstack/echo"

func booking(e *echo.Echo) {
	// group := e.Group("/books")

	// group.GET("/", GetBookings)
	// group.GET("/:id", GetBooking)
	// group.GET("/status/:status", GetBookingsByStatus)
	// group.POST("/", CreateBooking)
	// group.PATCH("/:id", UpdateBooking)
	// group.DELETE("/:id", DeleteBooking)
}
