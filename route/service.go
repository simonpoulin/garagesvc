package route

import "github.com/labstack/echo"

func service(e *echo.Echo) {
	// group := e.Group("/services")

	// group.GET("/", GetServices)
	// group.GET("/:id", GetService)
	// group.POST("/", CreateService)
	// group.PATCH("/:id", UpdateService)
	// group.DELETE("/:id", DeleteService)
}
